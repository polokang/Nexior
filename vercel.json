{
  "version": 2,
  "routes": [{ "handle": "filesystem" }, { "src": "/(.*)", "dest": "/index.html" }],
    "env": {
    "NODE_VERSION": "18"
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ]
}
